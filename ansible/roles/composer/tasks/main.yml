#roles/composer/tasks/main.yml
---
- name: COMPOSER - download composer
  shell: curl -sS https://getcomposer.org/installer | php

- name: COMPOSER - make composer global
  shell: mv composer.phar /usr/local/bin/composer